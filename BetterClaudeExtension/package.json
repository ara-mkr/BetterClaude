{
  "name": "betterclaude-extension",
  "version": "0.1.0",
  "description": "BetterClaude packaged as a Manifest V3 browser extension.",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "zip": "npm run build && node zip.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "chokidar": "^3.6.0",
    "adm-zip": "^0.5.18"
  }
}
